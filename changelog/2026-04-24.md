# 2026-04-24

## 今日已完成

- 复盘生产模式启动日志，定位悬浮窗右上角关闭按钮无效的直接原因：`preload` 脚本未成功加载，导致渲染层的 `window.hermesDesktop` 不可用，标题栏按钮点击后没有实际 IPC 调用。
- 修复 Electron preload 模块格式问题：将 `electron/preload.ts` 改为 `electron/preload.cts`，输出 CommonJS 的 `preload.cjs`，与 Electron 预加载脚本加载方式保持一致。
- 更新 `electron/main.ts`，让 `BrowserWindow` 指向 `dist-electron/electron/preload.cjs`，避免继续加载 ESM 形式的 preload。
- 调整主进程 renderer 日志兼容写法，避免 `console-message` 事件签名变化引发新的 TypeScript 构建错误。
- 更新 `tsconfig.electron.json` 以纳入 `.cts` 文件编译，并确认 `npm run build:main` 通过。
- 按当前产品决策调整标题栏行为：右上角 `X` 改为真正关闭应用，不再隐藏到托盘；新增 `window:close` IPC，并把前端关闭按钮接到该动作。
- 修正 Hermes 模型显示逻辑：去掉误导性的硬编码模型下拉框，改为直接读取 `~/.hermes/config.yaml` 的实际 `provider/default model` 并展示当前运行配置。
- 顺手推进技能面板落地：读取 `~/.hermes/skills/` 中已安装技能，替换原来的纯演示技能列表，让左侧面板更接近真实 Hermes 环境。

## 当前判断

- 关闭按钮“无效”的根因不是按钮样式或点击事件本身，而是 preload 加载失败后，渲染进程拿不到 Electron 暴露的桥接 API。
- 在 preload 修复后，标题栏的最小化、隐藏到托盘、置顶切换逻辑都应该恢复正常。
- 当前标题栏右上角 `X` 的设计语义已经改为“真正关闭应用”；托盘仍保留显示/隐藏与退出入口。
- 当前模型区域已经不再假装“可切换”，而是明确显示 Hermes 当前真实配置；后续如果要支持切换，需要接入 `hermes model` 或配置写回流程。

## 今日接下来的工作准备

- 重新执行 `npm run build` 与 `npm run electron:preview`，验证 preload 修复后标题栏按钮是否恢复。
- 重新验证右上角 `X` 是否按预期直接退出应用，而不是隐藏到托盘。
- 继续处理 Electron 生产模式下的安全与体验问题，例如 Content Security Policy。
- 继续落地右侧工作区能力，优先做真实文件树和更接近 Hermes session 的上下文展示。
- 评估后续是否要把模型切换真正接入 `hermes model` 或配置写回，而不是只做展示。
- 为项目补充 `README.MD`：整理项目定位、能力概览、目录结构、安装/开发/构建方式与基础使用说明，方便后续交接与自用。
- 新增 `.gitignore`，避免把 `node_modules`、构建产物、日志和本地环境文件纳入版本记录。
- 初始化本地 Git 仓库并保存当前实现快照，便于后续继续迭代与回滚。
- README 改为中文为主，并补充中文快速上手说明，方便后续自用与交接。
- 将当前前端界面的核心文案切换为中文，包括标题栏、面板标题、会话状态、错误提示、工作区互操作提示与欢迎语。
- 将 `package.json` 项目描述改为中文，保持仓库元信息与当前中文化方向一致。
- 把工作区文件树从前端硬编码占位改为主进程真实读取当前项目目录，并通过 IPC 回传嵌套树结构在右侧面板展示。

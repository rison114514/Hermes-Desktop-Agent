# 2026-05-09

## 今日已完成

- 将运行路径调整为 Windows 原生 Electron 前端优先，Hermes 后端继续在 WSL2 中通过 `wsl.exe` 启动，并以 ACP stdio 作为主通信链路。
- 重写 `PLAN.md` 和 `README.MD` 的运行说明：Windows 原生为默认入口，WSLg 仅保留为开发或回退路径。
- 验证 Windows 原生窗口中文输入法候选框可用，避免继续依赖 WSLg 窗口承载前端。
- 重构 `electron/hermes-bridge.ts`：支持 ACP `initialize`、`session/new`、`session/list`、`session/load`、`session/prompt`，并能在切换 workspace 后重启后端和创建新 session。
- 修正 ACP 流式输出处理：支持无 id 的 assistant delta，避免 Hermes 已回复但前端不显示的问题。
- 重构 Windows host 互操作层：在 Windows 主进程中使用 Electron clipboard/shell/API，WSL 仅用于路径转换、Hermes config、skills 和 ACP 后端执行。
- 修正 Windows host 下文件树、文件预览、Explorer 打开、剪贴板读写和 workspace 路径边界检查。
- 新增历史 session 下拉框：通过 ACP `session/list` 读取历史 ACP session，加载时按 session 的 `cwd` 切换工作目录并调用 `session/load`。
- 重新实现 worktree 能力：支持列出 git worktree、切换到指定 worktree，并在新目录中立即启动新的 ACP session。
- 新增 worktree 创建流程：创建后自动进入新目录、刷新文件树、重置聊天视图，并启动新的 Hermes ACP session。
- 支持自定义 worktree 名称，名称会规范化为 `hermes/<name>` 分支。
- 支持自定义 worktree 工作目录：目标目录通过 Windows 原生目录选择器选择，选择结果作为 `git worktree add` 的实际路径。
- 修复 worktree 创建脚本的 WSL/bash 引号问题，将复杂内联 shell 改为分步 `wsl.exe` 参数调用，避免 `sed` 和 root 解析被打碎。
- 修复 `windowsPathToWslPath()` 对 `/mnt/...` 和 `\\wsl...` 路径的处理，避免切换 worktree 后路径被错误转换。
- 新增 chat store 的 session 重置能力，使加载历史 session、创建 worktree、切换 worktree 后前端聊天视图与后端 session 保持一致。
- 将 `.worktrees/` 加入 `.gitignore`，避免自动创建的 worktree 目录污染版本状态。

## 当前判断

- 桌面端已经进入“Windows 前端 + WSL Hermes 后端 + ACP 主链路”的架构，后续功能应优先围绕 ACP session/workspace 语义推进。
- Hermes ACP session 的工作目录来自 `sessions.model_config.cwd`，可以支撑历史 session 下拉加载和目录切换。
- worktree 的产品语义已经明确：创建或切换 worktree 都应启动新的 ACP session，而不是复用旧 session。
- Windows 与 WSL 路径必须持续显式区分：Windows UI/Explorer/clipboard 使用 Windows path，Hermes/ACP/tool 执行使用 WSL POSIX path。

## 后续工作准备

- 在最新 Windows Electron 窗口中完整验证：自定义 worktree 名称、目录选择器、创建后自动切换、新 session 回复链路。
- 继续补齐 worktree 管理能力，例如删除/清理 worktree、显示 dirty 状态、显示分支和提交摘要。
- 继续完善历史 session 加载后的消息回放展示，确保 ACP replay 事件在前端能形成清晰的历史上下文。
- 评估是否需要持久化桌面端最近 workspace/worktree，以便重启后恢复上次目录。

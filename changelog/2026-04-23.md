# 2026-04-23

- 初始化 `Hermes Desktop Agent` 项目骨架，补齐 `electron/`、`src/`、`changelog/` 目录。
- 创建 `package.json`、TypeScript 配置、Vite 配置和 `index.html`，定义 Electron + React + Vite 开发/构建脚本。
- 实现 Electron 主进程、`preload` 安全桥接、Hermes 子进程通信骨架，支持通过 IPC 发送消息并回传事件。
- 搭建 React 三栏基础界面：`SkillsPanel`、`ChatPanel`、`WorkspacePanel`，并接入 Zustand store。
- 添加今日工作日志文件，后续继续按日追加开发进度。
- 根据产品方向调整 `PLAN.md`：明确以“桌面悬浮助手优先”为目标，同时保留 WSL2 与 Windows 的互操作设计。
- 实现首版 `WSL2 -> Windows` 互操作骨架：新增路径转换、Explorer 打开能力，并在工作区面板展示 Windows 路径与入口按钮。
- 增加桌面助手窗口能力：系统托盘、关闭隐藏到托盘、`Super+H` 召回、窗口位置/尺寸/置顶状态持久化。
- 将窗口控制接入前端标题栏，支持切换 `always-on-top`、最小化和隐藏到托盘。
- 扩展 Windows 互操作：增加默认程序打开工作区、读取 Windows 剪贴板、写入 Windows 剪贴板，并在工作区面板提供对应入口。
- 改造 Hermes 聊天事件流：Bridge 侧增加更细粒度事件归一化，前端接入 assistant start/delta/done 状态，聊天区展示连接状态与流式消息。
- 安装依赖验证构建链路：由于 Electron 二进制下载超时，改用 `npm install --ignore-scripts` 完成依赖安装，并修复 TypeScript 构建错误，确认 `npm run build` 通过。
- 手动处理 Electron 二进制阻塞：直接下载 `electron-v37.4.0-linux-x64.zip` 并解压到 `node_modules/electron/dist/`，确认二进制与版本文件存在；同时修正 `package.json` 的 Electron 主入口路径。
- 将 Electron/WSLg 运行依赖、apt 安装命令、自检步骤和当前阻塞状态补充进 `PLAN.md`，方便后续在本机完成系统库安装后继续验证。
- 安装 WSLg/Electron 运行库后再次验证：`ldd` 已无缺失项，Electron 在脱离 CLI sandbox 的条件下可返回 `v37.4.0`，并可启动 `npm run electron:preview`；当前仍有 DBus 相关警告，需要后续结合实际桌面会话继续观察。
- 为 Electron 启动增加 `dbus-run-session` 兜底脚本，修复 WSLg 下 `/run/user/1000/bus` 缺失导致的大量 DBus 启动报错；重新验证后仅剩正常的会话总线激活日志。
- 修复 Electron 生产模式黑屏问题：将 Vite 构建 `base` 改为相对路径，避免 `file://` 加载 `dist/index.html` 时静态资源错误指向根路径 `/assets/...`。
- 为黑屏问题继续加诊断：在主进程输出 `did-fail-load`、`did-finish-load`、renderer console 与 render-process-gone 日志，便于定位是资源加载失败还是渲染进程崩溃。
